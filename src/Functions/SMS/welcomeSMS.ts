const welcomeSMS = (name: string): string => {
    return `Hello ${name}, As the founders of LOOP and on behalf of the entire team, welcome to The LOOP Village! Sincerely, Maria & Margot`;
};

export default welcomeSMS;
