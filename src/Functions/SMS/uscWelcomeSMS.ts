const uscWelcomeSMS = (name: string): string => {
    return `Hello ${name}, Thank you for signing up for LOOP/USC/LADOA’s The Power of Food Preparation. We will send a reminder 24-hours before the event and another 1-hour before with a link to the program.`;
};

export default uscWelcomeSMS;
