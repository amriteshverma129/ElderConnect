const usc1HourReminderSMS = (name: string, link: string): string => {
    return `Hello ${name}, LOOP/USC/LADOA’s The Power of Food Preparation will start in 1 hour. Here is the link: ${link}`;
};

export default usc1HourReminderSMS;
